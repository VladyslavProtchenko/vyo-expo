# 🚀 Инструкция по запуску проекта VYO

## ✅ Что уже сделано:

1. ✅ Восстановлено **110 файлов** из истории Cursor
2. ✅ Восстановлена папка `app/` со всеми компонентами
3. ✅ Восстановлены файлы OpenAI и Products
4. ✅ Установлены npm зависимости
5. ✅ Metro Bundler запущен на http://localhost:8081

## 📱 Запуск на iOS:

### Вариант 1: Через Xcode (рекомендуется)
```bash
# 1. Установите CocoaPods зависимости
cd ios
bundle install
bundle exec pod install
cd ..

# 2. Откройте проект в Xcode
open ios/VYO.xcworkspace

# 3. Выберите симулятор и нажмите Run (⌘+R)
```

### Вариант 2: Через командную строку
```bash
# Metro уже запущен, откройте новый терминал:
npm run ios
```

## 🤖 Запуск на Android:

```bash
# 1. Запустите Android эмулятор или подключите устройство

# 2. В новом терминале:
npm run android
```

## 🔧 Если Metro не запущен:

```bash
npm start
```

## 📋 Важные файлы восстановлены:

- ✅ `app/_api/openai.ts` - логика запросов к OpenAI
- ✅ `app/_store/products.ts` - список всех продуктов (94 продукта)
- ✅ `app/_store/phase.ts` - логика фаз цикла
- ✅ `app/Products/index.tsx` - главный экран Products
- ✅ `app/_components/ui/ButtonGradient.tsx` - градиентная кнопка
- ✅ `.env` - переменные окружения
- ✅ `Constants.ts` - константы проекта

## ⚠️ Важно:

1. **OpenAI API Key** находится в `app/_api/openai.ts` (строка 3)
2. **Supabase URL** в `Constants.ts`
3. Проект использует React Native 0.83.1 и React 19.2.0

## 🎯 Следующие шаги:

1. Настройте Google Sign-In credentials в `App.tsx` (строки 11-12)
2. Проверьте `.env` файл на наличие всех необходимых ключей
3. Запустите приложение на симуляторе/эмуляторе

---

**Проект успешно восстановлен из истории Cursor! 🎉**
